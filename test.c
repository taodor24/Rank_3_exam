
// d


// print

int backtrack(int *arr, skolko, target, index, int *subset, skolko, summa)
{
    if index==n
    {
        if summa==target
            print
        return
    }

    backtrack(int *arr, skolko, target, index+1, int *subset, skolko, summa);

    subset[skolko] = arr[index];
    backtrack(int *arr, skolko, target, index+1, int *subset, skolko+1, summa+arr[index])


}

int main(int ac, char **av)
{
    ...
    ...
    ...




    free
    free
    return
}